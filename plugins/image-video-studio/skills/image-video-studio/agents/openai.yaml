interface:
  display_name: "Image Video Studio"
  short_description: "Project KB and single-agent default workflow for the image-video SaaS"
  brand_color: "#6366F1"
  default_prompt: "Use $image-video-studio, load the project KB, and keep substantive image-video development in the current agent unless I explicitly ask for multi-agent delegation."

policy:
  allow_implicit_invocation: true
